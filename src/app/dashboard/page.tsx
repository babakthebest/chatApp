import Button from '@/components/ui/Button'
import {FC} from 'react'
import { db } from './../../lib/db';

type Props = {}

const Page: FC<Props> = ({ }) => {
 
  return (
    <Button > hello</Button>
  )
}

export default Page